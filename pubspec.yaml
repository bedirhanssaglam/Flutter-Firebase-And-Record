name: flutter_record_and_firebase_service
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.25.4
  cloud_firestore: ^4.15.4

dev_dependencies:
  flutter_test:
    sdk: fluttera
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
